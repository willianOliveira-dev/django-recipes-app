import * as LucideIcons from 'https://esm.sh/lucide';

const { createIcons, ...icons } = LucideIcons;

export function lucideDev() {
    createIcons({ icons });
}

export function refreshLucide() {
    createIcons({ icons });
}
